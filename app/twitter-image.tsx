// Twitter/X needs its own file convention even though the image is identical
// to the general Open Graph card — re-exporting avoids maintaining two copies.
export { default, size, contentType } from "./opengraph-image";
